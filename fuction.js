
// Defining functions 

function add (a, b) {
    let sum = a+b 
    return sum 
}

let total = add ( 3, 5 )

// Prints HELLO !!!!  ( Usa letra mayuscula y signos de exclamacion )
function shout ( text) {
    return text.toUpperCase() + '!!!!'
}

console.log(shout('hello'))
